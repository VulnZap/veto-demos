# Entered via the UI — these are only needed if running the backend standalone
MODEL_PROVIDER_TOKEN=
# Optional aliases:
# ANTHROPIC_API_KEY=
# VERTEX_API_KEY=
VETO_API_KEY=
VETO_BASE_URL=https://api.runveto.com
